import Consult from "./consult";

export const metadata = {
  title: "Book a Consultation | MiniquesCare",
  description: "See available slots and book a consultation with a pharmacist.",
};

export default function ConsultationPage() {
  return <Consult />;
}